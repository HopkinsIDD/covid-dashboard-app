import { combineReducers } from "redux";
import statsForMaps from "./statsForMap_reducers";

// This is the collection of all redux reducers to be used
export default combineReducers({
    statsForMaps
});
